const state = {
  isModalOpen: true,
  activeCoin: '',
  displaySuccess: false,
  isEditAsset: false,
};
const getters = {
  isModalOpen: (state) => state.isModalOpen,
};
const actions = {};
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
